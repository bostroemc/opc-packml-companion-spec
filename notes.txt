IDE: Visual Studio Code 
Extenstions: Quarto (quarto-vscode)



Command used to create output.md:
	quarto pandoc '.\OPC 30050 - UA Companion Specification for PackML 2.00-alpha_0.1.docx' -f docx -t markdown -s -o document.md

Extract media from Word document:
 	quarto pandoc '.\OPC 30050 - UA Companion Specification for PackML 2.00-alpha_0.1.docx' --extract-media media

Reference document (reference.docx) was created by copying 'OPC 30050 - UA Companion Specification for PackML 2.00-alpha_0.1.docx' and removing all content.  
This document was then post edited to adjust table formatting.  In document see style 'Table'.


Required yaml at top of markdown:

---
format:
  docx:
    output-file: "OPC 30050 - UA Companion Specification for PackML 2.00-alpha_autogenerated"
    reference-doc: reference.docx
---

With tooling properly configured, the following command will generated docuGenerate docx
	quarto render document.md  


GIT hints:

To create and immediately switch to new branch:
  git switch -c your-branch-name

To push changes in branch to remote (Github) repo:
  git push -u origin your-branch-name
 


GIT hints:

To create and immediately switch to new branch:
  git switch -c feature-branch

To push changes in branch to remote (Github) repo:
  git push -u origin feature-branch


To stage all changes to current branch:
  git add .

 To commit changes to current branch:
   git commit -m "description" 


After PR merge issue following to update local repo:

  git checkout main
  git pull
  git fetch --prune
  git branch -d feature-branch